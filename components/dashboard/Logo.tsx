interface LogoProps {
  status: boolean;
  image: string;
  alt: string;
}

export default function Logo({ status, image, alt }: LogoProps): JSX.Element {
  return (
    <>
     
      
    </>
  );
}
